export enum AuthPath {
  LOGIN = 'login',
  SIGN_UP = 'sign-up'
}

export enum MainPath {
  ALL = 'all',
  REGISTRY = 'registry',
  DENIED = 'denied'
}

export enum RegistryPath {
  ENTRIES = 'entries',
  CHANGES = 'changes'
}
