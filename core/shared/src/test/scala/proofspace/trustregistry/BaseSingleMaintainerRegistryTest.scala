package proofspace.trustregistry

class BaseSingleMaintainerRegistryTest extends munit.FunSuite {
  
  val address =  "test-cardano-address"; //generateCardanoAddress()
  
  test("build a single registry from specific transactions and generate a snapshot") {
    
    //val createTransaction =  createTransactionWith(address, "create")
    
    
  }
  
  
  
  
}
